<% if @book.errors.any? %>
  <h3 style="color: red;">
    <%= @book.errors.count %> error(s) prohibited this ojc from being saved:
  </h3>
  <p style="color: red;">
    <% @book.errors.full_messages.each do |message| %>
  </p>   
  <p style="color: red;"><%= message %></p>
     
    <% end %>
  
<% end %>


<h1>New book</h1>
<%= form_with model: @book, local:true do |f| %>
  <h4>Title</h4>
  <%= f.text_field :title %>
  <h4>Opinion</h4>
  <%= f.text_area :body %><br>
  <%= f.submit 'Create Book' %>
<% end %>